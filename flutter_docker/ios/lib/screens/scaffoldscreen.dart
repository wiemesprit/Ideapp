import 'package:flutter/material.dart';

class ScaffoldApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("generated app scaffold"),
      ),
      body: Center(
        child: Text("this is a generated app scaffold"),
      ),
    );
  }
}
